CMakeList scripts based on https://github.com/sun1211/cmake_with_add_subdirectory

To build and run: (Navigate into the cloned directory first)
```bash
cmake -S . -B build
cmake --build build
./build/bin/birdgame
```
